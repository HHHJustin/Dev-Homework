<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ .title }}</title>
</head>
<body>
    <h1>Student List</h1>
    <ul>
        {{ range .stuArr }}
        <li>{{ .Name }} ({{ .Age }} years old)</li>
        {{ end }}
    </ul>
</body>
</html>
